export class Media {
    async Upload(request) {
        console.log(`Received media upload request for ${request.url} and ${request.type}`);
        throw Error(`Media Plugin is only supported in Production - please use devvit upload instead of devvit start`);
    }
}
