import { SettingsRequest, SettingsResponse } from './settings.js';
interface Rpc {
    request(service: string, method: string, contentType: 'application/json' | 'application/protobuf', data: object | Uint8Array): Promise<object | Uint8Array>;
}
export interface SettingsClient {
    GetSettings(request: SettingsRequest): Promise<SettingsResponse>;
}
export declare class SettingsClientJSON implements SettingsClient {
    private readonly rpc;
    constructor(rpc: Rpc);
    GetSettings(request: SettingsRequest): Promise<SettingsResponse>;
}
export declare class SettingsClientProtobuf implements SettingsClient {
    private readonly rpc;
    constructor(rpc: Rpc);
    GetSettings(request: SettingsRequest): Promise<SettingsResponse>;
}
export {};
//# sourceMappingURL=settings.twirp-client.d.ts.map