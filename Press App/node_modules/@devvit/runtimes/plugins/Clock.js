import { Timestamp } from '@devvit/protos';
export default class Clock {
    // RPC
    async Now(_request, _metadata) {
        const timeMS = Date.now();
        return Timestamp.fromPartial({
            seconds: Math.floor(timeMS / 1000),
            nanos: (timeMS % 1000) * 1000,
        });
    }
}
