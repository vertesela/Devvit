import type { RemoteLogMessage } from '@devvit/protos';
import type { Observer } from 'rxjs';
type Console = {
    error(...args: unknown[]): void;
    log(...args: unknown[]): void;
};
type AppLogConfig = {
    dateformat?: string;
    json?: boolean;
    showKeepAlive?: boolean;
};
/**
 * RemoteLogMessage Rxjs Observer that logs messages to stdout / stderr for
 * users.
 */
export declare class AppLogObserver implements Observer<RemoteLogMessage> {
    #private;
    constructor(config: AppLogConfig, logger: Console);
    complete(): void;
    next(msg: RemoteLogMessage): void;
    error(err: unknown): void;
}
export {};
//# sourceMappingURL=app-log-observer.d.ts.map