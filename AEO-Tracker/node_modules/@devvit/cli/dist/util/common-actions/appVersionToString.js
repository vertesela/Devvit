export function appVersionToString(version) {
    if (!version) {
        return 'unknown';
    }
    let retval = `${version.majorVersion}.${version.minorVersion}.${version.patchVersion}`;
    if (version.prereleaseVersion) {
        retval += `.${version.prereleaseVersion}`;
    }
    return retval;
}
