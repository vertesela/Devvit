import type { FullAppInfo, AppInfo } from '@devvit/protos';
import { ProjectCommand } from '../util/commands/ProjectCommand.js';
export default class Publish extends ProjectCommand {
    #private;
    static description: string;
    static examples: string[];
    static args: {
        appWithVersion: import("@oclif/core/lib/interfaces/parser.js").Arg<string | undefined, Record<string, unknown>>;
    };
    run(): Promise<void>;
    /**
     * throws error if the app is not found, or if the user doesn't have permission to view the app
     */
    getAppBySlug(slug: string): Promise<FullAppInfo>;
    protected checkAppVersionTermsAndConditions: (app: AppInfo, appDetailsUrl: string) => Promise<void>;
}
//# sourceMappingURL=publish.d.ts.map