import { Command } from '@oclif/core';
class Version extends Command {
    async run() {
        this.log(this.config.version);
    }
}
Version.description = 'Display version information about the locally installed devvit CLI';
export default Version;
