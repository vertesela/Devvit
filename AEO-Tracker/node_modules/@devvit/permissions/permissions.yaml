permissions:
  - actor: runtime.system.local
    has:
      - SYSTEM
    requires:
      - permission: SYSTEM
  - actor: invoker.system.local
    requires:
      - permission: SYSTEM
  - actor: inspector.system.local
    requires:
      - permission: SYSTEM
  - actor: supervisor.system.local
    has:
      - SYSTEM
    requires:
      - permission: SYSTEM
  - actor: http.plugins.local
    has: # No permissions
    requires: # No permissions
  # Allow this one specific test actor to make HTTP requests so that we can
  # unit-test the HTTP plugin. to-do: add to app name block list.
  - actor: hello-world-request.local
    has:
      - HTTP
  - actor: redditapi.system.local
    has:
      - HTTP
