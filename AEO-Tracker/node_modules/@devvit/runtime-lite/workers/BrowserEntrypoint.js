import { UnsandboxedRuntimeLite } from '../runtime/UnsandboxedRuntimeLite.js';
import { assignRuntime, startRuntime } from './RuntimeLiteWorker.js';
// to-do: pull in https://github.com/stephenh/ts-proto/issues/732 to fork and
// use plain globalThis here and everywhere else in runtime-lite.
const box = (globalThis.self ?? globalThis);
assignRuntime(box, new UnsandboxedRuntimeLite());
startRuntime(box);
