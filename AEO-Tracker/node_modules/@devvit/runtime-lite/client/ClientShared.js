export function getAppName(bundle) {
    return bundle.hostname.split('.')[1] ?? bundle.hostname;
}
export function getAppTitle(bundle) {
    const appName = getAppName(bundle);
    return `${appName}@${bundle.actor?.version} by ${bundle.actor?.owner}`;
}
