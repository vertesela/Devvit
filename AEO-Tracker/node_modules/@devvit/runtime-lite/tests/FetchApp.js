export default class FetchApp {
    async Ping(ping) {
        console.log('ping');
        if (ping.message === 'Fetch') {
            const rsp = await fetch('https://example.com');
            console.log('fetched', rsp);
        }
        return ping;
    }
}
