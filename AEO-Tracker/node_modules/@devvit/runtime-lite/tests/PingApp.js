/** toString() on this class must be a valid program. */
export default class PingApp {
    // to-do: refactor Actor out of @devvit/runtimes and extend it.
    async Ping(ping) {
        console.log('Ping', ping);
        return ping;
    }
}
