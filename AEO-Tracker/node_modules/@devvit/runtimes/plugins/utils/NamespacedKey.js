import { Header } from '../../lib/Header.js';
export function namespacedKey(metadata, key) {
    // return key;
    const caller = metadata?.[Header.Caller]?.values[0];
    return caller == null ? key : `${caller}:${key}`;
}
