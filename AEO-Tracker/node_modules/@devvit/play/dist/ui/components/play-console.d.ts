import { LitElement, type CSSResultGroup, type TemplateResult } from 'lit';
import type { Diagnostic } from 'typescript';
import type { Diagnostics } from '../../types/diagnostics.js';
declare global {
    interface HTMLElementEventMap {
        'open-line': CustomEvent<OpenLine>;
    }
    interface HTMLElementTagNameMap {
        'play-console': PlayConsole;
    }
}
export type OpenLine = {
    /** One-based index. */
    line: number;
    /** Zero-based index. */
    char: number;
};
export declare class PlayConsole extends LitElement {
    diagnostics?: Diagnostics;
    static styles: CSSResultGroup;
    protected render(): TemplateResult;
    tsErrRow(err: Diagnostic): TemplateResult<1>;
}
