import { Envelope } from '@devvit/protos';
export interface EnvelopeFilter {
    /**
     * Transform an incoming envelope. Throws when the transform fails and cannot
     * return an Envelope.
     */
    filter(envelope: Envelope): Promise<Envelope>;
}
//# sourceMappingURL=EnvelopeFilter.d.ts.map