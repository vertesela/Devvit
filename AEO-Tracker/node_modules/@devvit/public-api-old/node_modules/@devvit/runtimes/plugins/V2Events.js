import { V2EventsPublishResponse, } from '@devvit/protos';
export class V2Events {
    async Publish(request, _metadata) {
        console.debug(`Received v2 events publish request: ${JSON.stringify(request)}`);
        return V2EventsPublishResponse.fromPartial({});
    }
}
