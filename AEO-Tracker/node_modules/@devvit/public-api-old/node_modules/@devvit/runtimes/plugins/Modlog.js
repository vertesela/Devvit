import { ModlogResponse, } from '@devvit/protos';
export default class Modlog {
    async Add(request, _metadata) {
        console.debug(`Received modlog request: ${JSON.stringify(request)}`);
        return ModlogResponse.fromPartial({
            success: true,
        });
    }
}
