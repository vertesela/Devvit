import { Metadata, SettingsRequest, SettingsResponse } from '@devvit/protos';
import type { Settings as _Settings } from '@devvit/protos';
import type { Dispatcher } from 'undici';
/**
 * SettingsPlugin loads settings from an HTTP endpoint that is specified in metadata as `devvit-sec-settings-uri`
 * Unlike the go runtime which implements a caching for scalability, this implementation will reload the
 * settings from the remote host whenever it is called.
 */
export default class Settings implements _Settings {
    #private;
    constructor(dispatcher?: Dispatcher);
    GetSettings(_request: SettingsRequest, metadata: Metadata | undefined): Promise<SettingsResponse>;
}
//# sourceMappingURL=Settings.d.ts.map