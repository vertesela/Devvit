import type { Metadata } from '@devvit/protos';
import { Empty } from '@devvit/protos';
import { KeySet, KVStore, ListFilter, MessageSet } from '@devvit/protos';
declare class LocalStorageKV implements KVStore {
    #private;
    constructor(localStorage: Storage);
    Get(request: KeySet, metadata: Metadata | undefined): Promise<MessageSet>;
    Del(request: KeySet, metadata: Metadata | undefined): Promise<Empty>;
    Put(request: MessageSet, metadata: Metadata | undefined): Promise<Empty>;
    List(_request: ListFilter, metadata: Metadata | undefined): Promise<KeySet>;
}
export { LocalStorageKV };
//# sourceMappingURL=LocalStorageKV.d.ts.map