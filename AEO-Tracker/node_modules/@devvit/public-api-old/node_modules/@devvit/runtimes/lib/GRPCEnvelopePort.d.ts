import { Definition, Envelope, Metadata } from '@devvit/protos';
import * as grpcWeb from 'grpc-web';
import { EnvelopeEventListener, EnvelopePort } from '../common/envelope/EnvelopePort.js';
export declare class GRPCEnvelopePort implements EnvelopePort {
    #private;
    onmessage: EnvelopeEventListener | null;
    constructor(remoteUrl: string, definitions: Definition[], extraOutgoingMetadata: Metadata | undefined, client?: grpcWeb.AbstractClientBase);
    postMessage(envelope: Envelope): void;
    terminate(): void;
}
//# sourceMappingURL=GRPCEnvelopePort.d.ts.map