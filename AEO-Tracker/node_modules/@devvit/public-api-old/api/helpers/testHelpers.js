import { Devvit } from '../../abstractions/Devvit.js';
export function mockGetApiClient(fns) {
    return vi.spyOn(Devvit, 'getApiClient').mockReturnValue({
        ...fns,
    }); // eslint-disable-line @typescript-eslint/no-explicit-any
}
