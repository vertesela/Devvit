import { LitElement, type CSSResultGroup, type TemplateResult } from 'lit';
declare global {
    interface HTMLElementTagNameMap {
        'play-dropdown-menu': PlayDropdownMenu;
    }
}
/**
 * @slot menu - Menu list items. to-do: extract menu component instead of
 *              assuming li.
 * @slot trigger - Button. to-do: use a button.
 */
export declare class PlayDropdownMenu extends LitElement {
    #private;
    direction?: 'down' | 'up';
    private isOpen;
    static styles: CSSResultGroup;
    protected render(): TemplateResult;
}
