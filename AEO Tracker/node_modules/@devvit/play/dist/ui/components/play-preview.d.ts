import type { LinkedBundle } from '@devvit/protos';
import { LitElement, type CSSResultGroup, type PropertyValues, type TemplateResult } from 'lit';
import type { ColorScheme } from '../../types/color-scheme.js';
import '@devvit/previews/dist/devvit-preview.js';
declare global {
    interface HTMLElementEventMap {
        'clear-errors': CustomEvent<undefined>;
    }
    interface HTMLElementTagNameMap {
        'play-preview': PlayPreview;
    }
}
/** The rendered output of Devvit program under various parameters. */
export declare class PlayPreview extends LitElement {
    #private;
    static styles: CSSResultGroup;
    bundle: Readonly<LinkedBundle> | undefined;
    previewWidth?: number;
    scheme?: ColorScheme;
    private _err;
    protected render(): TemplateResult;
    protected willUpdate(props: PropertyValues<this>): Promise<void>;
}
