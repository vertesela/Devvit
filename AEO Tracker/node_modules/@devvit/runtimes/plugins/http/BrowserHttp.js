import { HTTP } from './BaseHttp.js';
export class BrowserHTTP extends HTTP {
    constructor(fetcher, namespace) {
        super(fetcher, namespace);
    }
    assertEnvironment() {
        if (typeof window === 'undefined' || typeof window.fetch === 'undefined') {
            throw new Error('BrowserHTTP can only be provided by the browser runtime');
        }
    }
}
