import { HTTP } from './BaseHttp.js';
import { fetch as undiciFetch } from 'undici';
export class NodeHTTP extends HTTP {
    constructor(dispatcher) {
        // fetch is implemented by undici for node 16.5+
        super(undiciFetch, dispatcher);
    }
    assertEnvironment() {
        if (typeof global === 'undefined') {
            throw new Error('NodeHTTP can only be provided by a node runtime');
        }
    }
}
