import { Envelope } from '@devvit/protos';
import { EnvelopeEventListener, EnvelopePort } from '../envelope/EnvelopePort.js';
import { AnySocket } from './AnySocket.js';
/** Adapts a socket to an EnvelopePort. */
export declare class SocketEnvelopePort implements EnvelopePort {
    #private;
    readonly id: string;
    onmessage: EnvelopeEventListener | null;
    constructor();
    bindSocket(socket: AnySocket): Promise<void>;
    unbindSocket(): void;
    postMessage(envelope: Envelope): void;
    terminate(): void;
}
//# sourceMappingURL=SocketEnvelopePort.d.ts.map