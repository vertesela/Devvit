import { PortEnvelopeRecipient } from '../envelope/PortEnvelopeRecipient.js';
import { AnySocket } from './AnySocket.js';
import { SocketEnvelopePort } from './SocketEnvelopePort.js';
export declare class SocketEnvelopeRecipient extends PortEnvelopeRecipient {
    #private;
    readonly type?: 'client';
    get id(): string;
    constructor(type: 'client' | undefined, hostname: string, port?: SocketEnvelopePort);
    bindSocket(socket: AnySocket): Promise<void>;
    unbindSocket(): void;
    dispose(): Promise<void>;
}
//# sourceMappingURL=SocketEnvelopeRecipient.d.ts.map