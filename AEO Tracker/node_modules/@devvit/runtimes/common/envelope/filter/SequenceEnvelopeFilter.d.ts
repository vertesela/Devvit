import { Envelope } from '@devvit/protos';
import { EnvelopeFilter } from './EnvelopeFilter.js';
/**
 * Concatenates filters. When any filter throws, any remaining filters are not
 * invoked.
 */
export declare class SequenceEnvelopeFilter implements EnvelopeFilter {
    #private;
    constructor(filters: readonly EnvelopeFilter[]);
    filter(envelope: Envelope): Promise<Envelope>;
}
//# sourceMappingURL=SequenceEnvelopeFilter.d.ts.map