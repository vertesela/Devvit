import { ConfigForm } from '@devvit/protos';
/**
 * Helper class that provides a Builder-style interface for generating a UserConfigurable ConfigForm
 */
export declare class ConfigFormBuilder {
    #private;
    /**
     * Returns the constructed ConfigForm
     */
    build(): ConfigForm;
    /**
     * Add a STRING parameter to the configuration (single-line string)
     */
    textField(key: string, label: string, currentValue?: string): ConfigFormBuilder;
    /**
     * Add a NUMBER parameter to the configuration
     */
    numberField(key: string, label: string, currentValue?: number): ConfigFormBuilder;
    /**
     * Add a BOOLEAN parameter to the configuration
     */
    booleanField(key: string, label: string, currentValue?: boolean): ConfigFormBuilder;
    /**
     * Add a PARAGRAPH parameter to the configuration (multi-line string)
     */
    textarea(key: string, label: string, currentValue?: string): ConfigFormBuilder;
}
//# sourceMappingURL=ConfigFormBuilder.d.ts.map