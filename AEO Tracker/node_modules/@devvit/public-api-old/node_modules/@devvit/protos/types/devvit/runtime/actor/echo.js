/**
 * #echo.ts
 *
 * @packageDocumentation
 */
/* eslint-disable */
import _m0 from 'protobufjs/minimal.js';
import { Str } from '../runtime_common.js';
export class EchoClientImpl {
    constructor(rpc, opts) {
        this.service = opts?.service || "devvit.runtime.actor.Echo";
        this.rpc = rpc;
        this.Echo = this.Echo.bind(this);
    }
    Echo(request) {
        const data = Str.encode(request).finish();
        const promise = this.rpc.request(this.service, "Echo", data);
        return promise.then((data) => Str.decode(new _m0.Reader(data)));
    }
}
export const EchoDefinition = {
    name: "Echo",
    fullName: "devvit.runtime.actor.Echo",
    methods: {
        echo: {
            name: "Echo",
            requestType: Str,
            requestStream: false,
            responseType: Str,
            responseStream: false,
            options: {},
        },
    },
};
