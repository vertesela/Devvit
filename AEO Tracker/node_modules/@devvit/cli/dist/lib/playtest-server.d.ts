import type { PlaytestSubscriberMessage } from '@devvit/protos';
/**
 * Playtest development mode server. Connects PlaytestClients for bidirectional
 * messaging. See PlaytestClient.
 */
export declare class PlaytestServer {
    #private;
    constructor(dateFormat: string | undefined, logger: Logger);
    close(): void;
    open(): void;
    /** Send to all connections. */
    send(msg: PlaytestSubscriberMessage): void;
}
type Logger = {
    log(...args: readonly unknown[]): void;
    warn(...args: readonly unknown[]): void;
};
export {};
//# sourceMappingURL=playtest-server.d.ts.map