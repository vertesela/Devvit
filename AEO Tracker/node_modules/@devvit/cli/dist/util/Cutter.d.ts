/** Render a template directly to the filesystem. */
export default class Cutter {
    #private;
    source: string;
    constructor(source: string);
    cut(target: string, mustacheContext: unknown): Promise<void>;
}
//# sourceMappingURL=Cutter.d.ts.map